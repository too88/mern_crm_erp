import { Select } from "antd";

//TODO: complete search item component 
function SearchItemComponent({}) {
  return (
    <Select>
      <Select.Option></Select.Option>
    </Select>
  );
}

export default function SearchItem({}) {
  return <SearchItemComponent />;
}
